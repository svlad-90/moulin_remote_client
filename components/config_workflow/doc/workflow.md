# Configuration Workflow

The configuration workflow component wires configuration storage, host
configuration screens, project configuration screens, and app callbacks into
the controller used by the main menu.

Other components import this behavior through `components.config_workflow.api`.
