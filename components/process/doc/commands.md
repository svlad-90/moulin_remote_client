# Process Execution

`ProcessExecutionService` is the public process API for local subprocess
execution, command output capture, and process-group termination.

`src/commands.py` remains as the private backend used by the execution service.
